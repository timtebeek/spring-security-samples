server.port: 8081

spring:
  application:
    name: dummy-service
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9090/auth/realms/service/protocol/openid-connect/certs